# uso del break
 ````python
 
 while True:
    nombre = input('Ingrese su nombre... (Salir para terminar): ')

    if nombre == 'salir':
        break
    pritn ('Bienvenido: ', nombre)

print('Program finalizado')

 ````


 # Continue
``` python
 for producto in range(6):
    if producto == 3:
        continue
    
    print('Producto etiquetado: ', producto)
```

# Ejemplo:
print('='*15)
print('escaner de Productos')
print('='*15)

# Ejemplo de un break buscanod un producto peligroso
``` python

for caja in range (1, 11):
    print(f'Escneando caja {caja}')
    
    # simulamos que la caja 5 es un producto peligroso
    if caja == 5:
        print('alerta')
        break
print('el scaner ha terminado su revisio de seguridad.')


print('\='*15)
print('Proceso de Pagos')
print('='*15)

for cliente in range (1, 6):
    print(f'Procesando pago del cliente {cliente}')
    
    # Simulamos que el cliente 5 tiene un error en su pago
    # Fondo insuficiente, tarjeta vencida, etc
    
    if cliente == 5:
        print('Erro!!! ')
        continue
    
    print('pago Procesado exitosamente.')
```


# Estructura de datos
## Lista
## Craer una lista
###forma 1: con elementos
mi_lista = [10, 'hola', 3.14, ]

### forma 2: sin elementos
lista_vacia = []

## acceder a los elementos
### Dos pasos
mi_lista[1]
saludo = mi_lista[1]
print(saludo)

### un paso
print(mi_lista[1])

# Agregar elementos (`append`)
### sintaxis
`nombre de la lista` + `.` + `append(`  + ` lo que quiero agregar` + `)`
```python

# Recorrer una lista
## acceder a los elementos
### Dos pasos
mi_lista[1]
saludo = mi_lista[1]
print(saludo)

### un paso
print(mi_lista[1])

lista_vacia.append('Diego')

print(lista_vacia[0])

mi_lista.append('mundo')

print(mi_lista)

#forma 1: con un ciclo 'for'
for elemento in mi_lista:
    print(elemento)
    
    if elemento == 3.14:
        print('Encontre el numero pi.')
        break

    
# forma 2: Usando los indices
len(mi_lista) #Devuelve la cantidad de elementos que tiene la lista
for indice in range(len(mi_lista)):
    print(mi_lista[indice])
```

## forma 2: indices negativos
pritn(mi_list [-1])


## forma 3: Por porcion de la lista (slicing)
`Nombre de la lista` + `[ + inicio + : final + ]`
mi_lista[1:2]

## forma 4: slicing con saltos
`Nombre de la lista + [ + inicio + : final + : + paso/Incremtento + ]`

mi_lista[0:4:2]


# Modificar un elemento
mi_lista[1] = 'HOLA'


# Eliminar un elemento por elidice
## Eliminar un elemento por el indice con `del`

del mi_lista[1]

## Eliminar con valor con `remove{}`
`nombre de la lista` + `.` + `remove(` + `el valor que quiero eliminar`+ `)`

## Eliminar el ultimo elemento con `pop`
mi_lista.pop()


# Estructura de tuplas
**sintaxis** en luugar de corchetes`[]` (lista), usamos parentesis `( )` (tuplas)

## crear tupla
```python

productos = ('arroz', 1200, 5)
```

## acceder a valores
```python

print(productos[1])
print(productos[2])


```