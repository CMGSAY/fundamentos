

### Agregando habilidades exclusivas al hijo
```python
# La super clase (el padr)
# class Animal:
#     def comer(self):
#         print("El animal está comiendo.")
    
# class Perro(Animal):
#     pass

# mi_mascota = Perro()
# mi_mascota.comer()

class Vehiculo:
    def encender_motor(self):
        print('[Sistema] | El motor se ha encendido')

    def apagar_motor(self):
        print('[Sistema] | El motor se ha encendido')
# Subclase 1(hijo)
class Auto(Vehiculo):
    def encender_aire(self):
        print('[AUTO] | Aiere acondicionado es encendido')

# Subclase 2(hijo)
class Moto(Vehiculo):
    def hacer_acrobacia(self):
        print('[Moto] | Levantar la rueda delantera')

carro = Auto()
moto = Moto()

print('\nacciones del Autro')
carro.encender_motor()
carro.encender_aire()

print('\nacciones del moto')
moto.encender_motor()
moto.hacer_acrobacia()
```

## Constructor del hijo
```python
class Padre:
    def __init__(self, apellido):

        self.apellido = apellido

class Hijo(Padre):
    def __init__(self, nombre):
        self.nombre = nombre

chico = Hijo('Carlos')


```

### Solucion
`Super()` 
`__init__()`

```python
class Persona:
    def __init__(self, nombre):
        self.nombre = nombre

class Estudinate(Persona):
    def __init__(self, nomre_ingresado, nota_ingresada)
    super().__init__(nombre_ingresado)
    self.nota = nota_ingresada

    def mostrar_info(self):
        print(f(Hola, mi nombre es: {self.nombre} y nota: {self.nota_ingresada}))


```


# Las muchas formas (Polimorfismo)
**SOLID** :
1. **S - Single Responsibility (Responsabilidad unica):**
2. **O - Open/Closed (Abierto y Cerrado):**
3. **L - Listov Substitution (Sustitution de Liskov):**
4. **I - Interface Segregation (Segregacion de Interfaces):**
5. **D - Dependecy Inversion (Inversion de Dependenciaas):**

```python
class EmpleadoDios:
    def __init__(self, nombre, salario):
        self.nombre = nombre
        self.salrio = salario
    
class CalculadoraFinanciera:
    def calcular_impuestos(self, empleado):
        return empleado.salario *0.13
    
class RepositorioBaseDeDatos:
    def gudardar(self, empleado):
        print(f'Conectando..')

```

## La Rebelion del hijo (Sobrescritura de Metodos)
```python
class PersonaMayor():
    def saludar(self):
        print('Buenas tardes estimado, como se encuentra usted el dia de hoy es un pracer saludarlo.)

class Adolecente(PersonaMayor):
    def saludar(self):
        print('Hola, todo bien?')

senor = PersonaMayor()
chico = Adolecente()

snor.saludar()
chico.salusar() 
```

### El poder del polimorfismo

#### Ejemplo | simulacion de granja
```python
class animal:
    def __init__(self, nombre):
        self.nombre = nombre
    
    def hacer_sonido(self):
        print(f'{self.nombre} Hace un sonido generico')

class Perro(Animal):
    def hacer_sonido(self):
        print(f'{self.nombre} | el perro hace: Guau, Guauuuu')

class Gato(Animal):
    def hacer_sonido(self):
        print(f'{self.nombre} | el gato hace: Miauu, Miaaauuuu')

class Pato(Animal):
    def hacer_sonido(self):
        print(f'{self.nombre} | el pato hace: Cuac, Cuaaac')

animal1 = Perro('Pochiberto')
animal2 = Gato('Porticio')
animal3 = Pato('Pancracio')


lista_granja = []
lista_granja.append(animal3)

animal_desconocido = Animal('Extraterrestre')

lista_granja.append(animal_desconocido)

print(lista_granja)
```

## Combinando lo viejo y lo nuevo
### 1. Usar `super()` en metodos normales

```python
class EmpleadoBase:
    def iniciar_rutina(self):
        print('1.')
```


## El gran debate: herencia vs polimorfismo
